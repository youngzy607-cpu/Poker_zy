# 部署指南 (Deployment Guide)

## 🚀 快速部署
本项目包含自动部署脚本，您可以根据您的操作系统选择使用：

### Windows 用户
双击运行 `deploy_simple.bat`。
*注意：脚本已配置为使用 `F:\Program Files\Git\cmd` 下的 Git。*

### Mac / Linux / Git Bash 用户
在终端运行：
```bash
./deploy.sh
```

---

## 🔧 手动部署
如果您更喜欢手动操作，请在终端依次执行以下命令：

```bash
# 1. 添加更改
git add .

# 2. 提交更改
git commit -m "Update game"

# 3. 推送至 GitHub
git push
```

**注意**：请确保您的电脑已安装 Git 并配置了环境变量。如果 `git` 命令无法识别，请检查 Git 安装路径。

## 🌐 访问游戏
代码推送成功后，请访问您的 GitHub Pages 链接（在 GitHub 仓库的 **Settings** -> **Pages** 中查看）。
