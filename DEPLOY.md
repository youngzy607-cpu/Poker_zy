# 部署指南 (Deployment Guide)

🎉 **Git 已成功安装并初始化！**

我已经帮您完成了大部分繁琐的配置工作：
1.  ✅ 安装了 Git (v2.52.0)。
2.  ✅ 初始化了本地仓库。
3.  ✅ 创建了 `.gitignore` 排除无关文件。
4.  ✅ 提交了所有代码 (v1.4)。
5.  ✅ 关联了您的 GitHub 远程仓库。

## 🚀 最后一步：推送代码
您现在只需在终端中运行以下**一条命令**，将代码推送到 GitHub：

```bash
    & "F:\Program Files\Git\bin\git.exe" push -u origin main
    ```
    
    *(注意：由于 Git 刚刚安装，您可能需要使用完整路径，或者重启 VS Code/Trae 以便直接使用 `git` 命令)*
    
    ---
    
    ## 常见问题 (FAQ)
    
    ### Q: 推送时提示需要登录？
    A: 这是正常的。Git 会弹出一个窗口让您登录 GitHub 账号，或者要求输入 Token。请按照提示操作即可。
    
    ### Q: 之后如何更新？
    以后每次修改完代码，依次运行：
    
    ```bash
    # 1. 添加更改
    & "F:\Program Files\Git\bin\git.exe" add .
    
    # 2. 提交 (记得写清楚改了什么)
    & "F:\Program Files\Git\bin\git.exe" commit -m "更新说明"
    
    # 3. 推送
    & "F:\Program Files\Git\bin\git.exe" push
    ```

### Q: 怎么让别人玩？
代码推送成功后：
1.  去 GitHub 仓库的 **Settings** -> **Pages**。
2.  将分支 (Branch) 设置为 `main` 并保存。
3.  您将获得一个链接（例如 `https://youngzy607-cpu.github.io/Poker_zy/`），发给朋友即可！
